---
import profile from '../data/profile.json';

interface Props {
	theme?: 'light' | 'dark';
}

const { theme = 'light' } = Astro.props;

const MAP_LAT = profile.location.lat;
const MAP_LON = profile.location.lon;
const MAP_LABEL = profile.location.label;
const MAP_ZOOM = profile.location.zoom;
---

<section id="contact" class={`contact-section ${theme}`}>
	<div class="shape-bottom">
		<div class="block-title">
			<div class="container-medium">
				<h2>Contact</h2>
				<div class="title-icon"><i class="fa fa-envelope-o"></i></div>
			</div>
		</div>
	</div>

	<div class="contact-area">
		<div class="form-contact-area">
			<div class="inner-contact">
				<ul class="contact-tabs" role="tablist">
					<li class="active" data-tab="tab-contact">Contact Me</li>
					<li data-tab="tab-hire">Hire Me</li>
				</ul>

				<div class="tab-content">
					<form
						id="tab-contact"
						class="tab-pane active"
						data-form="contact"
						action="https://formspree.io/f/xeebvyel"
						method="POST"
					>
						<input type="hidden" name="_subject" value="New message from portfolio contact form" />
						<div class="form-group">
							<label for="fullname">Your Name (*)</label>
							<input type="text" id="fullname" class="form-flat" name="fullname" required />
						</div>
						<div class="form-group">
							<label for="email">Email (*)</label>
							<input type="email" id="email" class="form-flat" name="email" required />
						</div>
						<div class="form-group">
							<label for="message">Your Message (*)</label>
							<textarea id="message" class="form-flat" name="message" rows="8" required></textarea>
						</div>
						<div class="form-group">
							<button type="submit" class="btn-solid">Send Message</button>
						</div>
						<p class="form-status" hidden></p>
					</form>

					<form
						id="tab-hire"
						class="tab-pane"
						data-form="hire"
						action="https://formspree.io/f/xeebvyel"
						method="POST"
					>
						<input type="hidden" name="_subject" value="New hire request from portfolio" />
						<div class="form-group">
							<label for="hire-fullname">Your Name (*)</label>
							<input type="text" id="hire-fullname" class="form-flat" name="fullname" required />
						</div>
						<div class="form-group">
							<label for="hire-email">Email (*)</label>
							<input type="email" id="hire-email" class="form-flat" name="email" required />
						</div>
						<div class="form-group">
							<label for="hire-message">Short Description About the Project (*)</label>
							<textarea id="hire-message" class="form-flat" name="message" rows="8" required></textarea>
						</div>
						<div class="form-group">
							<button type="submit" class="btn-solid">Send Request</button>
						</div>
						<p class="form-status" hidden></p>
					</form>
				</div>
			</div>
		</div>

		<div class="map-area">
			<button type="button" class="map-overlay" aria-label="Click to interact with the map">
				<span><i class="fa fa-map-marker"></i> Click to interact with the map</span>
			</button>
			<div id="leaflet-map" data-lat={MAP_LAT} data-lon={MAP_LON} data-label={MAP_LABEL} data-zoom={MAP_ZOOM}></div>
		</div>
	</div>
</section>

<style>
	.contact-section {
		position: relative;
		z-index: 1;
	}

	.contact-section.light {
		background: #ffffff;
	}

	.contact-section.dark {
		background: #323232;
	}

	.container-medium {
		max-width: 960px;
		margin: 0 auto;
		padding: 0 15px;
		position: relative;
	}

	.shape-bottom {
		position: relative;
	}

	.shape-bottom:before {
		content: '';
		position: absolute;
		width: 20px;
		height: 20px;
		z-index: 1;
		bottom: -10px;
		left: 50%;
		margin-left: -10px;
		transform: rotate(45deg);
		border-right: 1px solid #ebebeb;
		border-bottom: 1px solid #ebebeb;
	}

	.contact-section.light .shape-bottom:before {
		background: #f5f5f5;
	}

	.contact-section.dark .shape-bottom:before {
		background: #282828;
		border-color: #1e1e1e;
	}

	.block-title {
		padding: 40px 0;
		text-align: center;
		border-top: 1px solid #ebebeb;
		border-bottom: 1px solid #ebebeb;
	}

	.contact-section.dark .block-title {
		border-color: #1e1e1e;
	}

	.contact-section.light .block-title {
		background: #f5f5f5;
	}

	.contact-section.dark .block-title {
		background: #282828;
	}

	.block-title h2 {
		margin: 0;
		text-transform: uppercase;
	}

	.contact-section.light .block-title h2 {
		color: #000000;
	}

	.contact-section.dark .block-title h2 {
		color: #ffffff;
	}

	.title-icon {
		display: inline-block;
		position: relative;
		margin-top: 10px;
		font-size: 18px;
		color: #7d7d7d;
	}

	.contact-section.dark .title-icon {
		color: #919191;
	}

	.title-icon:before,
	.title-icon:after {
		content: '';
		position: absolute;
		height: 1px;
		width: 20px;
		top: 50%;
		background: #323232;
	}

	.title-icon:before {
		left: -30px;
	}

	.title-icon:after {
		right: -30px;
	}

	.contact-section.dark .title-icon:before,
	.contact-section.dark .title-icon:after {
		background: #cdcdcd;
	}

	/* == layout == */
	.contact-area {
		display: flex;
		flex-wrap: wrap;
	}

	.form-contact-area {
		flex: 1 1 480px;
		display: flex;
		justify-content: flex-end;
	}

	.inner-contact {
		width: 100%;
		max-width: 470px;
		padding: 60px 40px;
	}

	.map-area {
		flex: 1 1 480px;
		position: relative;
		border-left: 1px solid #ebebeb;
		min-height: 400px;
	}

	.contact-section.dark .map-area {
		border-color: #1e1e1e;
	}

	#leaflet-map {
		width: 100%;
		height: 100%;
		min-height: 400px;
		filter: grayscale(0.3);
	}

	.contact-section.dark #leaflet-map {
		filter: grayscale(0.5) invert(0.9) hue-rotate(180deg);
	}

	.map-overlay {
		position: absolute;
		inset: 0;
		z-index: 2;
		background: rgba(255, 255, 255, 0.7);
		border: 0;
		cursor: pointer;
		font-family: inherit;
		color: #000000;
		text-transform: uppercase;
		font-size: 12px;
		font-weight: 700;
		letter-spacing: 1px;
		transition: opacity 0.3s ease;
	}

	.contact-section.dark .map-overlay {
		background: rgba(30, 30, 30, 0.75);
		color: #ffffff;
	}

	.map-overlay.hidden {
		opacity: 0;
		pointer-events: none;
	}

	@media (max-width: 900px) {
		.form-contact-area {
			justify-content: center;
		}
		.map-area {
			border-left: 0;
			flex-basis: 100%;
		}
	}

	/* == tabs == */
	.contact-tabs {
		list-style: none;
		display: flex;
		gap: 4px;
		margin: 0;
		padding: 15px 10px 0;
		background: #f5f5f5;
		border: 1px solid #ebebeb;
		border-bottom: 0;
	}

	.contact-section.dark .contact-tabs {
		background: #282828;
		border-color: #1e1e1e;
	}

	.contact-tabs li {
		padding: 10px 15px;
		cursor: pointer;
		font-size: 13px;
		text-transform: uppercase;
		font-weight: 700;
		letter-spacing: 0.5px;
	}

	.contact-section.light .contact-tabs li {
		color: #000000;
	}

	.contact-section.dark .contact-tabs li {
		color: #ffffff;
	}

	.contact-tabs li.active {
		box-shadow: 0 -2px 0 #000000 inset;
	}

	.contact-section.dark .contact-tabs li.active {
		box-shadow: 0 -2px 0 #ffffff inset;
	}

	.tab-content {
		padding-top: 20px;
	}

	.tab-pane {
		display: none;
	}

	.tab-pane.active {
		display: block;
	}

	/* == form == */
	.form-group {
		margin-bottom: 15px;
	}

	.form-group label {
		display: block;
		margin-bottom: 8px;
		font-size: 13px;
	}

	.contact-section.light .form-group label {
		color: #000000;
	}

	.contact-section.dark .form-group label {
		color: #ffffff;
	}

	.form-flat {
		width: 100%;
		box-sizing: border-box;
		height: 45px;
		padding: 10px 12px;
		font-family: 'Open Sans', sans-serif;
		font-size: 13px;
		border: 1px solid #ebebeb;
		border-radius: 0;
		background: transparent;
		color: inherit;
	}

	textarea.form-flat {
		height: auto;
	}

	.contact-section.dark .form-flat {
		border-color: #1e1e1e;
		color: #ffffff;
	}

	.form-flat:focus {
		outline: none;
		border-color: #c3c3c3;
	}

	.btn-solid {
		min-width: 151px;
		padding: 13px;
		font-weight: 700;
		text-transform: uppercase;
		font-size: 12px;
		letter-spacing: 1px;
		border: 0;
		cursor: pointer;
		color: #ffffff;
		background: #323232;
		transition: background 0.3s ease;
	}

	.btn-solid:hover {
		background: #000000;
	}

	.contact-section.dark .btn-solid {
		background: #ffffff;
		color: #1e1e1e;
	}

	.contact-section.dark .btn-solid:hover {
		background: #ebebeb;
	}

	.form-status {
		font-size: 13px;
	}
</style>

<script>
	function initContactTabs() {
		document.querySelectorAll<HTMLElement>('.contact-tabs').forEach((tabs) => {
			const items = Array.from(tabs.querySelectorAll<HTMLLIElement>('li'));
			const panes = tabs
				.closest('.inner-contact')
				?.querySelectorAll<HTMLElement>('.tab-pane');
			if (!panes) return;

			items.forEach((item) => {
				item.addEventListener('click', () => {
					const targetId = item.dataset.tab;
					items.forEach((i) => i.classList.toggle('active', i === item));
					panes.forEach((pane) => pane.classList.toggle('active', pane.id === targetId));
				});
			});
		});
	}

	function initContactForms() {
		document.querySelectorAll<HTMLFormElement>('form[data-form]').forEach((form) => {
			form.addEventListener('submit', async (e) => {
				e.preventDefault();

				const status = form.querySelector<HTMLElement>('.form-status');
				const submitBtn = form.querySelector<HTMLButtonElement>('button[type="submit"]');
				if (!status || !submitBtn) return;

				submitBtn.disabled = true;
				status.hidden = false;
				status.textContent = 'Sending…';

				try {
					const response = await fetch(form.action, {
						method: 'POST',
						body: new FormData(form),
						headers: { Accept: 'application/json' },
					});

					if (response.ok) {
						status.textContent = "Thanks! Your message has been sent.";
						form.reset();
					} else {
						const data = await response.json().catch(() => null);
						const message = data?.errors?.map((err: { message: string }) => err.message).join(', ');
						status.textContent = message || 'Something went wrong. Please try again.';
					}
				} catch {
					status.textContent = 'Something went wrong. Please try again.';
				} finally {
					submitBtn.disabled = false;
				}
			});
		});
	}

	let leafletLoading: Promise<void> | null = null;

	function loadLeaflet(): Promise<void> {
		if ((window as any).L) return Promise.resolve();
		if (leafletLoading) return leafletLoading;

		leafletLoading = new Promise((resolve) => {
			const link = document.createElement('link');
			link.rel = 'stylesheet';
			link.href = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.css';
			document.head.appendChild(link);

			const script = document.createElement('script');
			script.src = 'https://unpkg.com/leaflet@1.9.4/dist/leaflet.js';
			script.onload = () => resolve();
			document.head.appendChild(script);
		});

		return leafletLoading;
	}

	async function initMap() {
		const mapEl = document.getElementById('leaflet-map');
		const overlay = document.querySelector<HTMLButtonElement>('.map-overlay');
		if (!mapEl) return;

		const lat = Number(mapEl.dataset.lat);
		const lon = Number(mapEl.dataset.lon);
		const label = mapEl.dataset.label ?? '';
		const zoom = Number(mapEl.dataset.zoom) || 13;

		await loadLeaflet();
		const L = (window as any).L;

		// Leaflet's auto-detected icon path breaks when leaflet.js is injected
		// dynamically (as above), leaving the marker + shadow icons 404ing.
		// Point them at the CDN explicitly instead.
		L.Icon.Default.mergeOptions({
			iconRetinaUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon-2x.png',
			iconUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-icon.png',
			shadowUrl: 'https://unpkg.com/leaflet@1.9.4/dist/images/marker-shadow.png',
		});

		const map = L.map(mapEl, { scrollWheelZoom: false }).setView([lat, lon], zoom);
		L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
			attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors',
			maxZoom: 19,
		}).addTo(map);
		L.marker([lat, lon]).addTo(map).bindPopup(label);

		overlay?.addEventListener('click', () => {
			overlay.classList.add('hidden');
			map.scrollWheelZoom.enable();
			map.invalidateSize();
		});
	}

	initContactTabs();
	initContactForms();
	initMap();
</script>
